import { useEffect } from "react";
import type ShapeManager from "../classes/Managers/ShapeManager";

export type collabRoomId = string;

export default function useCollab(
  shapeManager: ShapeManager,
  collabRoomId: collabRoomId,
) {
  //

  useEffect(() => {
    //
  });
}
